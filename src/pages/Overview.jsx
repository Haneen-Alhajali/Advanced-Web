import React, { useEffect } from 'react';

const Overview = () => {
  useEffect(() => {
    // Initialize charts or maps if needed
    console.log('Overview loaded');
  }, []);

  return <div>Overview Content</div>;
};

export default Overview;
